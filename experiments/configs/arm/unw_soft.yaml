# @package _global_
#
# Arm `unw_soft`: hyperparameters only. Anything shared with the other
# arms belongs in protocol/, and no key may appear in both.

arm_name: unw_soft
uses_preferences: true
uses_demonstrations: true
initial_queries_frac: 0.1

algo:
  kwargs:
    batch_size_expert: 64
    batch_size_pref: 256
    demo_weight: 1.0
    gcl_fusion: norm_balance
    gradient_steps_rew: 139
    initial_agent_timesteps: 20000
    l2_rew: 1.1265276323434602e-06
    label_smoothing: 0.0
    labels_type: soft
    lr_rew: 0.001154295698198038
    normalize_agent_reward: false
    pref_temperature: 20.0
    reward_model_kwargs:
      net_arch: [64, 64]
