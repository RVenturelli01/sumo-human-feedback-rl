run:
  # Path to the training run directory (relative to project root output/)
  dir: "output/2026-03-19_13-03-37_soft_demo_pref_qnet"

agent:
  # Path to the policy checkpoint (relative to project root)
  model: "output/2026-03-19_13-03-37_soft_demo_pref_qnet/models/policy_christiano"

eval:
  episodes: 100

hydra:
  run:
    dir: /tmp/hydra_eval/${now:%Y-%m-%d_%H-%M-%S}
  output_subdir: null
