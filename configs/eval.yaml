run:
  # Path to the training run directory (relative to project root output/)
  dir: "output/2026-03-22_17-04-17_env_reward_demo_s0"

agent:
  # Path to the policy checkpoint (relative to project root)
  model: "output/2026-03-22_17-04-17_env_reward_demo_s0/models/policy_christiano"

eval:
  episodes: 100

hydra:
  run:
    dir: /tmp/hydra_eval/${now:%Y-%m-%d_%H-%M-%S}
  output_subdir: null
