name: PPO

# ── RL policy (SB3 algorithm used as the learning agent) ──────────────────
algo: PPO
kwargs:
  policy: MlpPolicy
  learning_rate: 3.0e-4
  batch_size: 64
  n_epochs: 10            # gradient epochs per PPO update
  n_steps: 2048           # MUST be equal to n_rollout_steps