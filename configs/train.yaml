defaults:
  - algo: null
  - _self_


run:
  name: ""
  seed: 0
  output_dir: null


wandb:
  enabled: true
  kwargs:
    project: "shfre-train"
    group: ${env.id}_${env.kwargs.ego}_${env.kwargs.reward}
    name: ${run.name}
    mode: "online"
    dir: ${hydra:run.dir}/wandb
    sync_tensorboard: false
    notes: null


env:
  id: "HighwayEgo-v0"
  n_envs: 4
  kwargs:
    reward: "fast"
    ego: "discrete"


hydra:
  job:
    chdir: true
  run:
    dir: ${run.output_dir}/train/${now:%Y-%m-%d}_${now:%H-%M-%S}_${run.name}


