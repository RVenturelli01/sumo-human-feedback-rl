# What the plotting toolkit needs beyond what the rest of the repository already
# installs (pandas, matplotlib, wandb).
pyarrow>=14          # the cached index format (plots/.cache/run_index.parquet)
tikzplotlib==0.10.1  # .tex export via pgfplots; stopped in 2022, see rtplots/tikz.py:_compat()
webcolors==1.13      # tikzplotlib needs CSS3_HEX_TO_NAMES, removed in 24 and later
tomli>=2; python_version < "3.11"  # tomllib is only in the standard library from 3.11
